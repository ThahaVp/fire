<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
  <!-- Favicon-->
  <link rel="icon" type="image/x-icon" href="assets/favicon.ico" />
  
  <link href="/stylesheets/style.css" rel="stylesheet" />
    <link href="/stylesheets/map-styles.css" rel="stylesheet" />
    <link href="/stylesheets/bootstrap.min.css" rel="stylesheet" />

</head>

<body>
    <!-- Navbar -->
<nav class="navbar navbar-light bg-light">
  <div class="container-fluid">

WAIT ZERO
<div class="gap-2 d-md-flex justify-content-end">
  
  <a href="add-new-service"><button class="btn btn-light" type="button">Add Service</button></a>
  <button class="btn btn-dark me-md-2" type="button">Logout</button>
</div>


  </div>
</nav>
<!-- Navbar -->


<div class="container mt-5">
    <div class="row">
        <div class="col-md-12 col-lg-6">
      <div class="card mt-4">
      <div class="card-body">
        <h5 class="card-title">{{salesData.n}}</h5>
        <p class="card-text">{{salesData.id}} <br>
        {{salesData.a}}, {{salesData.d}}</p>
        <a href="#" class="btn btn-primary btn-sm">View Records</a>
      </div>
    </div>
    </div>
    {{!-- <div class="col-xl-8  col-lg-12 mt-3">
                    <div class="card">
                        <div class="card-header">
                            <h5 class="card-title" style="margin:0">Sales Users</h5>
                        </div>
                        <div class="card-body">
                            <div class="table-responsive text-nowrap">
                                <table class="table table-striped table-sm">
                                    <thead>
                                        <tr class="text-muted">
                                            <th>Date</th>
                                            <th>Title</th>
                                            <th>Area</th>
                                            <th>Type</th>
                                            <th>Status</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        {{#each data}}
                                        <tr>
                                            <td><span class="badge bg-secondary" style="cursor: pointer;">{{this.id}}</span>
                                            <td>{{this.n}}</td>
                                            <td>{{this.a}}</td>
                                            <td>{{this.d}}</td>
                                            <td class="text-muted">{{this.ll}}</td>
                                            <td class="text-muted">{{this.ct}}</td>
                                        </tr>
                                        {{/each}}

                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>   --}}

      {{!-- </div>   --}}
</div>


      <script src="/javascripts/jquery-1.12.4.min.js"></script>
    <script src="/javascripts/bootstrap.bundle.min.js"></script>
    
 
    
</body>

</html>